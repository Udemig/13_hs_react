import delay from "../../../utils/delay";

const RevenuePage = async () => {
  await delay(1500);

  return (
    <div className="box">
      <h1>Gelir Tablosu</h1>
    </div>
  );
};

export default RevenuePage;
